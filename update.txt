04-07-2026 updates

1. error occur in the subscription while assign the plan to the hospital when assign the enterprise plan.
2. register patient to patient registration in the entire application.
3. if i create the doctor in theri name should be display with the prefix Dr. before their name [Dr. Prefix in front of all doctors by default(in staff directory)]
4. remove the waiting text inside the current token that is display in the display queue.
5.While print the prescription in the top right corner of the page the status: Finalized (current one) change that to Prescribed.
6.opthal prescription is not saved it shows the error .make sure that the hospital have the optical store or if not in the both condition we can able to create the opthal prescription and that should save in the db perfectly.if that is needed or later the module is enabled, then all the details should show perfectly with out any backend and the frontend error.
7. in the entire application check all the quick action link,that should redirect to the mentioned and to that exact page ,whne the user click instead by default going to the dashboard(currently tht issue is happening).


--  Doubt

In walk in registration, create check box below choose doctor. and name it as specialist assignment. 

it should function as, if the check box is enabled , the patient will be consulted by that doctor alone. So in queue display his token number should be shown under that particular doctor only



-- 07-07-2026

1 . in inventory module the new purchse order is not working perfectly.
2.  i have the medicine named abc but it not show in the po medicine select options
3. in the sales section under the pharmacy module after the dispencing the status is still showing the pending ,analysis those functions and find the real cause for that and fix it.
4. could not able to dispense. if click dispense it is skipping
5. in the invoice section,cant able to create the new invoice for the details in the application
6. in the registration form,initially i have tried to select baby it is not selecting but after entering dob it is asking me to select baby/master.please check this it should allow initially and based on dob you have to cross verify


1. in the pharmacy queue what is purpose of the manual queue option .if its not necessary remove that
2. in the inventory module their is a action functions,but i cant able to understanf that one easy ,add some better thing for the easy understandable one.
3. in the grn, make sure that hte batch number should be edit after the Finalized the option.
4. in the invoice ,when i click the new invoice it shows the white space in the screen.
5. make sure that the system quantity is fixed one and that should be comes from the history or the store data form db.
6. also check the all the print option and the downlaod option in the invoice is works perfectly ,with the correct formate.

7.mainly focus on the stock and other things in the inventory, all these things in the section are mapped correct and works perfeclty by onr by one,because the each is depend on the before and the next one.


-- 08-07-2026


1. in the staff directory the search is not working
2. after creating the patient record ,i cant able to edit their details.i want the option to edit that as well and that should update in all the places.
3. failed to upload patient image
while creating patient i used already exists mail also allowed
while creating the staff it not validate both the user name and gmail .but now it validate the user name only
4. in the queue display i face this issue - some value are zero .but it correctly fetch the prescription ,no Reschedule appointment option in the appoinment
5. in the billing invoice i face this issue - not able to create new invoice.when i click it open the blank page. Default values applied when missing .System Configuration and Hospital Settings it not fille the default values
6. the data mention in this prormpt is for explain,check the logic and fix it 
    In pharmacist login 5 prescriptions had been dispensed out of 7 but still it shows pending prescription as 7
7. in the sales -> In cashier login the unpaid and partially paid payments are shown as paid status And there isn't a option to receive payments by the cashier for pending payments



-- need review

1. mail service issue.


-- 10-07-2025

1. consultation fee for the doctor is not calculated and not maintain the perfect record in the application.the action are only shown in the list with any filters,if i apply any fileter ,the action are hide out.also the clear option is not working perfectly and their is 2 option ,cause conflicts.also keep the filter options size that will match to all other filter options.
2. some the router redirect to the other module and the function.everytime not the same, one sometimes some new module and the section face errors.
3. doctor create the prescription and it is the final one.at the time of deispence,give the flexible to add some other extra things are medicine at the time of dispencing.also can able to add some time that are not related to medicine but available i the pharmacy.
4. email validation ,currently its a optional one,allow that field to get input as a text and make that field into a flexiable one.dont add more number of constraints to that field.
5. at the time of dispencing show the status of the patientif they paid the consulation fee or not.but dont make that is necessaary to pay the consulation fee first before go for the prescription.keep the flexiblity.
6. i face some issue in the hospital details conflicts while create the user with same name in another hospital.so make sure everythings are works based on the multi tenant id,because it is the first level of filters.
7. in each login show the role of that user in full visible one .in the left bottom corner ,the role and name is vsisble.instread of that place add the visiblity in the top right corner of the each user login page.keep the exisiting drop down as it is near to that left add this details (between notification and the drop down).
8. in the prescription output,if the prescription constains atleast one eye category medicine then add the column re and le.if their is not eye medicine category ,dont add the column at the time of view and print .not at the time of creating the prescription.



---  11-07-2026


1. In patient directory, for Patient please generate AI avatar for their id card based on child, man, woman,old man,old woman
2. Enable queue display for receptionist login and also here the 6 patient is in waiting list but it is noted as Next in line. Make it as Waiting.
3. The token number is assigned based on the doctor’s my patient queue but it should be assigned based on the patient queue
For example. The patient with token 2 is visiting doctor 1 as 2nd patient then he is visiting doctor 2 as 1st patient means it should not show in queue display as 1st patient. From start to end if he is token number 2 he is the token number 2 for all the doctor,pharmacy,billing etc…




Fixed

1. superadmin option shown in the admin ,staff register form.
2. view icon in the patient directory under the action column. -> need the view test for the asy understanding
3. token refersh one status -> (for the everyday it refersh and start from the count one).
4. in the dispencing the splitup for the tablete price oksy.but ui want the some more clear one for the easy visible and the understanding.
5. still now i cant able to understand the logic in the total.paid,balance and the amount filed.give the simple explaination about that and how the logic works by together.    
6. make sure to collect the collect the consultation fee at the single time itself.
7. in the doctor dashboard ,the consulation fee is shown as 0 ,but the today collected works perfectly.check that and fix tht as well.
8. In all payments, the amount collected and refunded back but the net collected is showing as 1600 it the example bug.
also in the ui theri is a more empty space in left and right side of the table view.fix that  ui issue as well. also tht show be flex one and suitable to different screen size.



-- db need to update in the live server
    06 new
    07 new








-- review

1. department and the specilization difference and need?





BUG-12 — Consultation/Today's Collected showing ₹0
Root cause: get_doctor_today_summary() computed "today's collected" by finding appointments dated today and summing whatever their invoices had ever collected — completely missing payments made today for appointments from other days (e.g., a backlog of unpaid visits getting settled). I verified this against live data for a real doctor: the old query returned ₹300, the corrected one (keyed off Payment.payment_date == today instead of appointment date) returned ₹1,060 — ₹760 of real cash collected today was being silently dropped. Also changed the "Consultation ₹0" fee badge to show "Fee not set" instead of a misleading ₹0 when a doctor's rate was never configured (the DB defaults it to 0, not null).

BUG-21 — Refer-to-doctor
The "Refer to Doctor" action was a dead-end: it called the referral API directly without saving the referring doctor's own clinical notes/medicines/optical Rx first, silently discarding anything typed. Fixed by making it a real "Save & Refer to Doctor" action — it now saves any drafted content first (skipping the "must have a medicine" requirement, since referring without prescribing is valid) and only proceeds to refer if that succeeds. Also pre-fills the Referral Reason field from the doctor's clinical notes instead of requiring a retype.

BUG-23 — Batch dispensing quantity mismatch
The quantity input let a pharmacist type up to the medicine's total stock across all batches, not the selected batch's own stock — so entering a quantity larger than the chosen batch could hold triggered a silent cross-batch split on the backend (which does auto-FEFO-split) with zero visibility to the pharmacist. Added a clear breakdown panel that shows exactly which batches (soonest-expiring first, confirmed the backend already sorts this way everywhere) will supply the shortfall, so nothing is silent anymore. Applied to both prescribed items and extra items.

BUG-25 — Invoice missing Returned/Change amount
Found the exact gap: the Totals section only rendered a "Balance Due" row when balance_amount > 0; when a patient overpays (balance goes negative), that row just disappeared with no substitute — confirmed against a real overpaid invoice in the DB (Total ₹200, Paid ₹210, Balance -10, silently showing nothing). Added a "Returned (Change)" row for the negative case.

BUG-26 — Consultation fee not collected in billing
The system already knew the fee was outstanding — it just showed a warning telling staff to go collect it separately at reception instead of collecting it itself. Since DispensingBilling.tsx already builds a real Invoice at payment time, I now prepend a "Consultation Fee" line item to that same invoice (using the combined invoice type that already supports mixing consultation + medicine items) and void the stray standalone consultation invoice that prescription-finalization auto-creates, so it's never double-billed. The Total, Cash Received, and Change now include it automatically.

BUG-27 — Net collected wrong after refund; no refund invoice
The "Net Collected" bug itself was already fixed earlier this session (PaymentList.tsx). Digging further, I found the same refund-blindness bug freshly baked into today's BUG-12 fix: get_doctor_today_summary summed raw Payment.amount for 'completed' payments, but a partially refunded payment stays 'completed' (only a full refund flips it to 'reversed') — so it overstated collections. Extracted a shared payment_net_amount() helper (amount minus reserved refunds) into payment_service.py and used it in both places, so this can't drift apart again. Also built the missing piece: a /refunds/{id}/pdf printable refund receipt (mirroring the existing invoice PDF), with a "Print Receipt" button wired into both InvoiceDetail.tsx and RefundList.tsx.

BUG-28 — Stock not restored after refund
Confirmed: process_refund only ever touched money (Invoice.paid_amount, Payment.status) — it never touched MedicineBatch.quantity or logged a StockMovement, because a Refund had no way to reference which line item or how many units were being returned. Added that link (nullable invoice_item_id + restock_quantity on Refund, migration written but not run per your standing rule), let staff pick "Medicine Being Returned" in the Request Refund modal, and on processing, restore the quantity to the exact batch it was dispensed from (traced via medicine + batch number) with a proper StockMovement audit entry.

BUG-30 — FIFO ordering
Found it in one line: get_pending_prescriptions sorted Prescription.created_at.desc() — newest first, the exact opposite of FIFO — which is why a patient waiting 20+ hours was buried under one waiting 5 minutes. Changed to ascending order on finalized_at (falling back to created_at), matching the same timestamp the on-screen "wait time" column already uses.

-- Medium 

BUG-04 — Username template. New backend endpoint GET /users/suggest-username builds HospitalCode + First2(FirstName) + First2(LastName) + _ + 3-digit sequence (e.g. besasa_001). The 3-digit suffix is a per-hospital running counter, matching your example where the second user gets _002 regardless of name. The create-staff form now auto-fills the username from the names (debounced, backend-owned sequence so two admins can't mint the same number), replacing the old email-prefix logic.

BUG-06 — Duplicate patient detection. First/last name now require minimum 2 letters. When a full 10-digit mobile is typed in registration, a debounced lookup runs — if a patient already has that number, an amber warning shows their name + registration number, and clicking the PRN opens their record. Warning only, never blocks (families share phones).

BUG-11 — Staff role. Removed from the assignable-roles dropdown (your decision). Existing "staff" users keep working; no new ones can be created.

BUG-16 — Analytics gating. New "In-house doctor — allow Analytics access" checkbox in the doctor section of Add/Edit staff (defaults on). Backing it: analytics_enabled column on doctors (migration 08 — written, not run), carried through create/update APIs and /doctors/me. The sidebar hides Analytics for doctors with the flag off. Receptionist access kept unchanged per your decision.

BUG-19 — Queue display. "Next in Line" now shows exactly one token (the actual next patient); everyone behind them moved to a new muted "Waiting" section. Receptionists get the Queue Display sidebar link (admins already had it). On the security note: I verified the public endpoint returns only {token, status} — no patient names/details — and the link lives inside an authenticated receptionist session.

BUG-22 — Pharmacist tools. One line was the whole bug: {!isPharmacist && ...} deliberately hid Add Medicine / Bulk Upload / Download Template from pharmacists while the routes already allowed them. Gate removed.

BUG-24 — Open stock in edit. The medicine edit form now shows an "Open Stock" section listing every batch with an editable quantity; changes save together with the medicine. (Stock lives on batches, so that's where it's edited.)

BUG-31 — Multi-GRN. The backend already tracked cumulative received quantities and partially_received status — the GRN form just never showed those POs (it filtered status=approved only) and pre-filled the full ordered quantity. Now: partially-received POs stay selectable (marked "balance pending"), items pre-fill with the remaining balance only, fully-received lines are dropped, and a note explains the pre-fill.

BUG-32 — Inventory list. Rebuilt to the reference layout: Code (SKU) | Product Name | Batch No | MFG Date | EXP Date | HSN | Current Qty | MRP | Safety Stock | Status — with expiry highlighted when <90 days and a proper In/Low/Out status badge. Generic/category/strength removed from the list (still on the View detail screen). Added a stock-status filter alongside search/category. Backend now enriches each row with its earliest-expiring in-stock batch (FEFO — the batch that dispenses next).

Cycle Counts disabled application-wide, code kept intact:

Backend: main.py's app.include_router(inventory.cycle_counts_router, ...) commented out — every cycle-count endpoint now 404s. The router, service functions, schemas, and models are untouched.
Frontend: the three /inventory/cycle-counts* routes in App.tsx are commented out (a stray link now just redirects to the dashboard via the catch-all route), the "Cycle Counts" nav item removed from Layout.tsx, and the quick-action tile removed from InventoryDashboard.tsx (grid resized from 7 to 6 columns to match). CycleCountsPage.tsx and CycleCountDetailPage.tsx are untouched — re-enabling is just uncommenting the lines I marked.
BUG-33 — inventory list now matches the Mecandria reference more precisely:

Added the missing Min Stock column (reads from the same reorder_level as Safety Stock — the schema only has one reorder threshold today, not two independent floors, so both columns intentionally mirror each other exactly like every row in your reference image does)
MRP Value column now has the light amber highlight from the reference, header renamed from "MRP" to "MRP Value"
Currency formatting now adds thousand separators (₹30,000.00 instead of ₹30000.00), matching the reference's grouped format

-- Low

BUG-05 — Salutation vs. gender. The gap: child titles (Baby/Master) already auto-corrected against gender, but adult titles (Mr./Mrs./Ms.) had zero cross-check — so picking "Mr." then switching gender to Female (or vice versa) just silently stuck. Fixed both directions: mismatched options are now disabled in the dropdown, and a new effect auto-corrects + toasts if gender changes after a title was already picked. Dr./Prof. stay untouched (gender-neutral).

BUG-07 — Analytics off for receptionist. Removed from both the sidebar gate (Layout.tsx) and the actual route guard (App.tsx's /analytics route) — the nav link alone wasn't the real gate, so both needed the change to actually block direct navigation.

BUG-08 — "Walk-in Registration" → "OPD Assignment". Pure label swap, 7 spots (sidebar nav ×2, dashboard quick-actions ×2, page heading ×3). Route path (/appointments/walk-in) and all logic untouched.

BUG-10 — Superadmin off Staff Directory's role list. Scoped narrowly: only Staff Directory's own "All Roles" filter dropdown excludes it. The shared Add/Edit Staff modal (used by both Staff Directory and User Management) is untouched, so creating a super_admin account — which only works through that one gated form — still works exactly as before.

BUG-13 — Formulation Search. Root cause: the medicine search doctors use when prescribing only matched name/generic name/manufacturer — typing "500mg" or "syrup" or an ingredient found nothing. Extended the search (both the hospital-scoped and the global-formulary search functions) to also match strength, dosage form, and composition. The Formulary Search panel in Prescription Builder now shows a category/dosage-form badge next to each result so doctors can tell formulations apart at a glance.

BUG-17 — Doctor Schedule for receptionist. The route guard alone wouldn't have been enough — the page's doctor-picker was hard-gated to isAdmin, so a receptionist landing there would've seen a blank "select a doctor" screen with no doctors to select. Broadened that gate to include receptionist, added the route to allowedRoles, and added the nav link.